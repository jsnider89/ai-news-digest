services:
  ai-news-digest:
    build: .
    container_name: ai-news-digest
    restart: unless-stopped
    ports:
      - "${PORT:-8002}:${PORT:-8002}"
    networks:
      - ai-news-digest-network
    volumes:
      - ./data:/data
      - ./config:/app/config
    env_file:
      - .env
    environment:
      - DATA_DIR=/data
      - DATABASE_URL=sqlite+aiosqlite:///data/app.db
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${PORT:-8002}/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  ai-news-digest-network:
    driver: bridge

volumes:
  data:
    driver: local