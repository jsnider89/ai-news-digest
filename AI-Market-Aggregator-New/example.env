# Environment variables for the AI Market Intelligence Aggregator
# Rename this file to .env and fill in the values for local development.
# DO NOT commit the .env file to version control.

# --- Email Configuration ---
# Used to send the final report.
# For Gmail, you may need to create an "App Password".
SENDER_EMAIL="your_email@example.com"
SENDER_PASSWORD="your_app_password"
RECIPIENT_EMAIL="recipient_email@example.com"

# --- API Keys ---
# You need at least one AI provider key.
# The system will use the primary provider defined in ai_config.yml first,
# then fall back to others if they are available.

# Finnhub API Key (for market data)
FINNHUB_API_KEY="your_finnhub_api_key"

# AI Provider Keys (at least one is required)
OPENAI_API_KEY="your_openai_api_key"
ANTHROPIC_API_KEY="your_anthropic_api_key"
GEMINI_API_KEY="your_gemini_api_key"
